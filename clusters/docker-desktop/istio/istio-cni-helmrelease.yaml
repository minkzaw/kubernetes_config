apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: istio-cni
spec:
  releaseName: istio-cni
  chart:
    spec:
      chart: cni
      version: "1.25.1"
      sourceRef:
        kind: HelmRepository
        name: istio 
  interval: 5m 
  install:
    remediation:
      retries: 3
  values:
    profile: ambient
